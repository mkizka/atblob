{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "_syncPnpm": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "build": {
      "dependsOn": ["^build", "_syncPnpm"],
      "outputs": ["dist/**"]
    },
    "typecheck": {
      "dependsOn": ["^build", "_syncPnpm"],
      "outputs": []
    },
    "lint": {
      "dependsOn": ["^build", "_syncPnpm"],
      "outputs": []
    },
    "format": {
      "dependsOn": ["^build", "_syncPnpm"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build", "_syncPnpm"],
      "outputs": []
    }
  }
}
